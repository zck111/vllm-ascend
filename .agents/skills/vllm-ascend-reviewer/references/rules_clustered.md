# 规则聚类分析（共 536 条）

## 统计概览

| 类别 | 规则数 | 占比 |
|------|--------|------|
| 模型与推理 | 99 | 18.5% |
| 配置与环境变量 | 88 | 16.4% |
| 文档与注释 | 75 | 14.0% |
| 代码规范与风格 | 66 | 12.3% |
| 硬件适配 | 57 | 10.6% |
| 错误处理与日志 | 39 | 7.3% |
| 版本兼容与同步 | 27 | 5.0% |
| 性能优化 | 17 | 3.2% |
| 构建与部署 | 16 | 3.0% |
| 测试与CI | 15 | 2.8% |
| 未分类 | 11 | 2.1% |
| KV Cache与存储 | 10 | 1.9% |
| 类型与接口设计 | 6 | 1.1% |
| 内存与张量管理 | 5 | 0.9% |
| 并发与分布式 | 3 | 0.6% |
| 安全与健壮性 | 2 | 0.4% |

## 模型与推理 (99 条)

- **每个新增的模型实现文件都应配套相应的单元测试（UT），确保模型的基本功能正确性。** [PR10042]
- **新增量化类型时，应复用已有的 `act_quant_type` 字段来表示激活值的量化类型，避免重复定义。同时在配置判断中需结合 `weight_quant.t** [PR10236]
- **在 MTP 推理中，相邻步骤的 top-k indices 高度重叠（70%-100%），可以通过共享 target model 的 `topk_indices** [PR10510]
- **在 model_runner_v1 `sample_tokens` / `_bookkeeping_sync` 等包含 `ModelRunnerOutput` ** [PR10727]
- **当通过 padding dummy request 修复 FIA 错误时，应验证 padding 不影响模型精度（通过 benchmark 测试），并在代码注释** [PR10769]
- **在投机解码的 `compute_draft_token_ids` 中，不同 method（eagle3/dflash vs 其他）使用不同的 logits 计算** [PR10781]
- **在 VL 模型中开启 FlashComm1/2 时，算子分发逻辑应通过前缀检查排除 `vision_model` 层，确保 VIT 层退回标准 TP 模式，避免** [PR10941]
- **凡是代码中出现形如 `if feature_enabled_and_requested_by_user and capability_present: do_f** [PR10977]
- **在 W4A16（weight 4-bit, activation 16-bit）量化方案中，激活值保持 16-bit 精度，不需要调用 `npu_dynamic** [PR11014]
- **具有通用性的辅助函数不应定义在特定业务模块（如 model_runner）内部，应移至 `utils.py` 等公共模块，提高代码可复用性。** [PR11065]
- **在处理特定模型架构（如 Gemma4）的量化配置时，辅助函数的命名应当与上游代码保持一致，以提高代码的可读性和可维护性。特别是涉及 k_eq_v 等特殊布局时，** [PR11575]
- **在开发新功能（如 MXFP 量化 token dispatch）时，不应修改与功能无关的代码（如 `device_op.py` 中的其他逻辑）。如果其他人已经修** [PR11614]
- **在 speculative decoding 的 graph 管理中，prefill 和 decode 的命名应准确反映其语义。DFlash 并行 drafti** [PR11895]
- **启动 Triton kernel（或 CUDA kernel）的 Python wrapper，必须在计算 grid 之后、launch kernel 之前检查** [PR11925]
- **当 compact KV all-gather 同时服务于 sparse-C8（packed KV，nope/rope/quantization 打包在 `kv** [PR11980]
- **当引入统一的 TP weight switch 机制并以其 `supports_tp_weight_switch` 标志位作为准入条件时，所有此前已支持 o-p** [PR12982]
- **当为 KV 缓存、block table、或 token 预分配预算选择 fallback 链时，最末一级"实在拿不到"之前，必须经过一次"批处理/单次调度维度** [PR13047]
- **在 attention metadata 构建中，`num_input_tokens` 应使用与当前执行模式（eager/cudagraph）一致的未 padd** [PR13069]
- **从 vLLM 上游移植的内核中，`num_tokens_running_state` 等运行态量有明确的计算公式（如 `new_num_computed - n** [PR13477]
- **当存在 `idx_mapping` 将连续 `batch_idx` 映射到稀疏 `req_idx`（请求状态槽位）时，针对 `num_reqs`（活跃 batc** [PR13477]
- **在 recompute/重试路径中向 multimodal content（list 形式，含 image/text 等多类型 part）追加生成的 token** [PR14241]
- **OpenAI 兼容 API 中，消息的 `content` 字段可以显式为 `null`（Python `None`），常见于 assistant 消息（带 t** [PR14241]
- **在端到端测试中，应使用 VLLMRunner 替代手动创建推理引擎的方式，VLLMRunner 能够自动清理内存，避免测试间的内存泄漏问题。** [PR2453]
- **在对 tensor 进行 shape 解包时，应确保包含所有维度，特别是 batch 维度，避免因维度遗漏导致的形状错误或计算错误。** [PR2528]
- **在使用 NPU 硬件算子（如 `npu_fused_infer_attention_score`）前，应确认该算子对目标场景（如 prefill、decode、** [PR2758]
- **在复用模型权重（如将 lm_head 和 shared_head 共享）时，应添加数值校验逻辑，确保两个权重在数值上一致，避免因量化或其他处理导致的精度下降。** [PR2790]
- **在添加 pooling 模型支持时，应正确使用 `AttentionType`（如 ENCODER_ONLY）来标识模型类型，而非将其混入 `AscendAtt** [PR3122]
- **在 NPUModelRunner 中，不同注意力机制（如不同 attention group）不应共享可变张量（如 `slot_mapping`），应各自从对应** [PR3572]
- **卷积层（conv/conv1d）的权重不应被转换为 FRACTAL_NZ 格式，因为这会导致精度问题。在 `process_weights_after_load** [PR4245]
- **添加新的量化支持（如 w8a16）时，测试用例名称应准确反映量化方法，且必须包含 e2e 精度校验。** [PR4541]
- **KV cache 的保存操作必须在模型 forward pass 完全完成后才能触发，需要确保数据完整性。无效的跨线程 synchronize 调用应当删除。** [PR4574]
- **从其他模型复制张量操作逻辑时，必须验证输入张量的 shape 是否一致，避免 torch.cat 等操作导致维度错误。** [PR4667]
- **model_runner 重构时应使用统一的 buffer 初始化方式，确保与上游 vLLM API 对齐，异步操作应使用 non_blocking。** [PR4764]
- **在 attention 实现中，基于 batch size 切换 PA/FIA 等注意力模式时，需确保逻辑仅在目标模式（如 full graph mode）下生** [PR4765]
- **在对模型权重进行 NZ 格式转换（`maybe_trans_nz`）或 `torch_npu.npu_format_cast_` 等操作时，必须通过 `.dat** [PR4878]
- **在 E2E 测试用例中，如果不使用 `VllmRunner`，则必须手动清理 NPU HBM 以避免内存泄漏。建议优先使用 `VllmRunner` 替代直接使** [PR4893]
- **在 MTP（Multi-Token Proposing）与异步调度和 ACL graph 配合使用时，需确保：1）避免依赖循环变量泄漏，显式定义变量；2）当 i** [PR4963]
- **当函数内定义一个变量名（尤其包含 token/step/request 这类常见单复数的词）时，所有后续引用、循环 unpack、数组赋值右侧的拼写必须与定义点** [PR5051]
- **在分布式连接器中，VL 模型可能有不同的初始化需求，应通过 early return 避免不必要的属性初始化。** [PR5103]
- **内存 profiling 和模型 warm-up 必须在推理模式下运行（`@torch.inference_mode()`），避免梯度追踪导致的额外内存占用和 ** [PR5136]
- **端到端测试中应使用 VllmRunner 提供的便捷方法（如 `generate_greedy`），避免手动创建 sampling_params 和 outpu** [PR5141]
- **Attention mask（如 sliding window attention mask）应在 AttnMaskBuilder 中统一管理和缓存，避免在每次** [PR5239]
- **提交代码时应使用平台原生 API（如 NPU API），移除无用的注释代码，保持代码缩进对齐，并还原调试期间的所有临时修改。** [PR5271]
- **batch_invariant 体系分两层：
- **顶层 `vllm_ascend/batch_invariant.py` = 调度层**：只定义 `vllm** [PR5517]
- **vLLM / vLLM-Ascend 的 `prepare_inputs / set_forward_context` 系列函数里，`attn_metadata** [PR5720]
- **凡是 vllm-ascend 引入的"V2 ModelRunner 才需要"的 forward context 钩子函数（`set_additional_for** [PR5720]
- **The initialization variables related to PCP are moved to attention_cp.py.** [PR6091]
- **在数据并行（DP）场景中，集合通信（如 all-reduce）要求所有 rank 的 tensor 大小完全一致。不能使用实际 token 数截断 tensor** [PR6715]
- **在 MLA（Multi-head Latent Attention）内核代码中，所有维度参数（如 qkNopeHeadDim、kvLoraRank、qkRope** [PR6902]
- **不同模型变体的特定逻辑应放在对应的 patch 文件中（如 `patch_qwen3_5.py`），不应放在通用的父类 patch 文件（如 `patch_qw** [PR7368]
- **在解析层名称获取层索引时，应使用项目已有的 `extract_layer_index` 工具函数，而不是手动编写解析逻辑。同时，判断逻辑应尽量简化，使用公开 A** [PR7628]
- **remove `The newly`, no model will keep `new`** [PR7804]
- **plz add new mode into https://github.com/vllm-project/vllm-ascend/blob/main/.git** [PR7886]
- **当某个功能模块（如 model_runner_v2）有多个 e2e 测试用例时，应将这些测试放在独立的子目录中，而不是散落在通用测试目录下。** [PR7922]
- **对于需要在 ModelRunner 中添加的平台特定功能（如 PCP），应将相关逻辑封装到独立的 Manager 类（如 `PCPManager`）中，而不是直** [PR8038]
- **在 KV cache 量化和传输逻辑中，应通过变量明确判断是否为 kvcache 量化场景，并根据注意力机制类型（MLA、GQA 等）进一步区分处理逻辑，避免硬** [PR8174]
- **req_idx: Check whether the value exceeds the length of cu_num_logits. If yes, mo** [PR8443]
- **1. The logits are split and then merged only when lmhead_tp_enable() is enabled.** [PR8718]
- **Will this affect non-GDN models? We have `impl_cls.update_graph_params` specific** [PR8842]
- **add some comments to explain why we need to use AttentionBackendEnum.FLASH_ATTN** [PR9060]
- **please make a rfc to track the developing plan,because we also need to implement** [PR9152]
- **```suggestion if not vllm_version_is("0.20.2"):     from vllm.models.deepseek_v4** [PR9399]
- **why we also need to check `batch_memcpy_kernel`?** [PR9514]
- **![critical](https://www.gstatic.com/codereviewagent/critical.svg)  The `npu_tran** [PR9531]
- **在 compressed-MLA 的准入控制中，`max_admission_blocks_per_request` 的额外 margin 应为 +1（对齐头部** [PR9548]
- **在 DSA 实现中，`_forward_prefill` 和 `_forward_decode` 中对同一功能（如 IndexCache 更新）的条件 guar** [PR9562]
- **新增量化方法（如 FP8）实现时，必须添加类说明文档，分析上游 vLLM 实现的差异，并确认修改不会影响其他模型。** [PR9757]
- **部署文档中的性能表格应使用 vLLM 标准字段名（Max Num Batched Token、Max Num Seqs、Max Model Len）。不应硬编码** [PR10460]
- **在模型部署文档中，不应添加与上级标题内容重复或无实质内容区分的嵌套小标题。小标题应当有明确的内容区分，避免层级过深导致阅读体验下降。** [PR10611]
- **模型部署文档的章节标题应遵循统一模板（如第9章应为"Performance Evaluation"而非"Performance Tuning"），确保文档结构一** [PR11010]
- **所有 `docs/source/tutorials/models/*.md` 下的模型部署文档（单模型教程），必须与项目标准化部署模板在以下 9 个维度保持一致** [PR11884]
- **更新 speculative decoding 配置时，除了标准的 decode 节点配置，还应检查 PD disaggregation 场景下 prefill** [PR12152]
- **批量替换标点符号（如全角逗号→半角逗号）时，容易误删相邻 token 之间的分隔符，替换后需逐一检查。** [PR12510]
- **当模型发布包（如 Eco-Tech/Kimi-K3-w4a8、Kimi-K3 ModelSlim 等）已经在 checkpoint 的 `compressed-** [PR13143]
- **对于存在大量重复前缀的推理场景（如多请求共享相同 system prompt 的对话部署），部署教程应推荐启用 APC（`--enable-prefix-cac** [PR14325]
- **精度测试报告模板应包含 vLLM 版本（带 commit 链接）、软件环境（CANN、PyTorch、torch-npu 版本）、硬件环境、并行模式和执行模式等** [PR2279]
- **教程文档应遵循统一的文件命名规范（如以模型名命名），不应重复创建已有内容。环境准备部分应包含模型权重信息，性能测试应使用真实数据集并与 CI 参数保持一致。** [PR3516]
- **调试和分析工具类依赖应放在 `requirements-dev.txt` 中，不应加入生产环境的 `requirements.txt`。文档示例应使用最新的模型** [PR3756]
- ****检查点**: 使用 `xxx/xxx` 格式的模型名称时，vllm 会尝试从 huggingface 或 modelscope 搜索，如果实际是本地模型需使** [PR4399]
- **教程文档中的模型名称必须与 modelscope/huggingface 等官方源上的名称完全一致（包括大小写），避免在大小写敏感的文件系统上出错。** [PR4666]
- **模型部署文档中的启动命令必须包含必要的环境变量设置（如 `OMP_NUM_THREADS=1`、`PYTORCH_NPU_ALLOC_CONF=expandab** [PR7292]
- **在模型部署文档中，启动推理服务前应设置 `export OMP_NUM_THREADS=1`，避免 OpenMP 线程与推理框架线程冲突导致性能下降。** [PR7296]
- **所有模型部署文档在启动推理服务前应统一设置 `export OMP_NUM_THREADS=1`，这是项目文档规范的一部分（参见 PR #7296 和 PR #** [PR7371]
- **模型文档的 FAQ 章节应仅包含该模型特有的问题，不应包含通用问题（如 HCCL 端口冲突、OOM 等），通用问题应放在通用 FAQ 文档中。** [PR9969]
- **在 P-D 分离部署的文档中，应明确说明 Prefill 节点和 Decode 节点在 MTP（Multi-Token Prediction）配置上的差异和最佳** [PR9995]
- **精度测试的 YAML 配置文件应使用官方模型源（如 `mistralai/`、`microsoft/`），不应使用第三方 re-upload 模型（如 `AI-** [PR3823]
- **E2E 测试重构时，应将同一模型的测试用例合并到统一的正确性测试中，以便同时对比 eager、aclgraph、hf 等多种推理模式的输出结果。** [PR4789]
- **plz add VLLM_USE_MODELSCOPE: true** [PR8680]
- **CI workflow 中的 vLLM 版本引用（commit hash、release tag）应集中在 `.commit` 文件中管理，避免在多个 work** [PR9801]
- **当 CPU tensor 和设备 tensor 之间存在 dtype 不匹配时，不应在每次推理循环（decode phase）中进行类型转换，而应在初始化阶段统** [PR10205]
- **在 `forward()` / `sample()` / `attention()` / `MoE_apply_mlp()` / `add_lora_fused** [PR10977]
- **当多个 attention metadata builder 在同一模型执行步骤内被构造时，若它们共享一组 SAS metadata 字典，不能在每次 `bui** [PR13155]
- **在每步推理都会执行的热路径（如 attention metadata builder）中，`torch.where(cond, x, fallback)` 的 ** [PR13779]
- **当逻辑依赖元素的特定顺序（如 decode 请求在 prefill 请求之前）时，不应遍历 dict 等无序集合，而应遍历保证顺序的集合（如 `input_ba** [PR4098]
- **在模型前向推理的热路径中，应避免重复计算可在初始化或 metadata 构建阶段预计算的值（如 cumsum），以减少不必要的计算开销。** [PR4228]
- **在 FIA（Flash Infer Attention）prefill context merge 路径中，应避免不必要的 tensor 操作（如转置、类型转换** [PR7293]
- **在 speculative decoding 的异步调度中，`mode=FULL` 下仅需要在两个 step 之间（draft model 完成后、下一个 ta** [PR8354]
- **当模型重写 `load_weights` 并加入自定义预处理（如 QuaRot 矩阵乘、量化重排）时，应补充纯 CPU 可运行的 UT，覆盖：被处理分支（如 F** [PR12262]
- **在 tests/e2e/weekly/**/*.yaml 的 `_server_cmd` YAML alias 中，所有 `--<kebab-case-long** [PR12796]

## 配置与环境变量 (88 条)

- **当从适配器/工厂方法获取的 kwargs 字典可能已包含某个键时，不应在 `dict()` 构造中再次显式传递该键，应使用 `setdefault` 或先检查键** [PR10026]
- **新增的配置验证方法（如 `_validate_parallel_config`）必须在 `tests/ut/` 目录下添加对应的单元测试，参考已有的同类测试模式** [PR10178]
- **当某个环境变量的值可以从已有配置中推导出来时，应自动计算并设置该环境变量，而不是要求用户手动配置。特别是对于分布式编译/推理场景中的关键变量（如 `LOCAL_** [PR10191]
- **配置数据中的映射关系如果未被实际使用，应及时清理。在处理压缩块的掩码过滤时，需要基于 `end_idx` 而非 `start_idx` 进行判断，避免跳过部分重** [PR10393]
- **Triton 操作中不应使用硬编码的硬件参数（如 vector core 数量），应通过动态查询接口获取，以适配不同硬件配置。** [PR10482]
- **在重构和整合测试套件时，需要仔细确认每个测试用例是否仍需要保留，避免误删有效的模型测试覆盖。** [PR10513]
- **在 `ascend_config.py` 中新增的配置项，必须在 additional_config 文档中添加对应的说明，方便用户理解和使用。** [PR10518]
- **在对不支持的 CLI 参数进行静默重置时，条件判断应使用 truthy 检查（直接检查参数值是否为 `True`），而非 `is not None`。因为 `F** [PR10602]
- **当融合算子已支持特定参数（如 swiglu_limit）时，应直接使用融合算子而非小算子拼接。独立模块（如独立 proposer）不应强制兼容不相关的基类逻辑，** [PR10697]
- **assert 语句的错误消息应明确说明触发的条件上下文（如特定的配置模式、参数组合），而不仅仅是描述"什么不能为空"，以便用户在遇到断言失败时能快速定位问题根因** [PR10846]
- **在 hamming 稀疏注意力模式下使用 ACL 全图重放时，必须按层覆盖 seq_lens 为缩减后的长度，确保重放行为与 eager 模式一致。同时应检查所** [PR10895]
- **当多个条件互斥时（如不同的 QuantType），应使用 `elif` 而非连续的 `if`，避免后续条件的 `else` 分支覆盖前面已正确设置的值。** [PR11287]
- **单元测试应有明确的功能覆盖范围说明，不应添加功能不清晰的测试代码。对于排除特定前缀的逻辑，应确认其在所有模型中的通用性。** [PR11433]
- **在 vLLM-Ascend 中，`fuse_qknorm_rope` 等 fusion 相关配置项属于 `AscendCompilationConfig`，不应** [PR11776]
- **当两个模式/标志/配置在实现层面语义互斥（例如 synthetic 要求 first-reject 逐 token，但 block_verify 走 cumpr** [PR12256]
- **不要在代码中添加基于魔法值（如 `eps == 1e-6`）的特殊分支而不加说明。这类特判路径使行为依赖于隐含的参数值，难以维护和测试。应统一调用路径，确保所有** [PR12772]
- **读取 vLLM 配置类环境变量时，必须使用项目统一的 `vllm.envs` 访问器（如 `envs_vllm.VLLM_EXECUTE_MODEL_TIMEO** [PR13233]
- **当某个分支路径会以 `None` 传入某些指针参数（如 `num_scheduled_tokens_ptr`）时，对这些指针的 `tl.load`/指针运算必须** [PR13477]
- **当存在 `enabled`（主开关）和 `need_timing`（子开关）这类语义依赖关系时，配置初始化阶段必须检测"主开关关闭但子开关开启"的矛盾配置，记 ** [PR13790]
- **当函数形参声明为 `cache_config: CacheConfig | None = None`（允许 `None`），但函数体内直接 `cache_con** [PR14375]
- **PR 应专注于单一功能或修复。如果发现了仓库级别的配置问题（如 mypy 配置），应创建独立的 PR 处理，不应在当前 PR 中混入。** [PR2167]
- **在修复 bug 或重构时，不应随意删除 CI 配置中的测试条目。即使某些测试暂时失败，也应保留配置并通过注释或 skip 标记处理，确保测试覆盖不丢失。** [PR2560]
- **在 CI 配置文件中引用上游仓库的 commit 时，应使用完整的 commit hash 而非短 hash，避免因 hash 冲突或 checkout 工具限** [PR2907]
- **在对 MoE gating top-k 算子进行泛化时，不应将算子调用变为无条件执行，需保留对不同 MoE 配置（如 softmax/sigmoid、group** [PR2958]
- **在 ACL graph capture 场景下为 attention metadata 添加 padding 时，应通过 `attn_state` 控制 pad** [PR3256]
- **变量命名应准确反映其实际用途（如 `mtp_layer_idx` 而非 `debug_layer_idx`）。配置项之间的关联应明确，避免一个配置隐式设置另一个** [PR3495]
- **`hf_config` 等共享配置对象应在模型层中视为不可变，禁止直接修改其属性，避免影响其他使用同一配置的模块。** [PR3833]
- **当移除某个功能（如 multicast）后，必须检查并清理所有因此变得无用的参数和代码路径。** [PR4224]
- ****检查点**: 从环境变量获取的值用作字典 key 前，必须检查该值是否存在于字典中** [PR4359]
- **当在 `check_and_update_config` 中已经覆盖了某个配置值时，不应在其他位置重复检查。同时，访问嵌套配置对象前应先检查其是否为 None。** [PR6330]
- **当子类 dataclass 新增了 `required=True`（无默认值）的字段，同时重写了一个基类里定义的 `@classmethod`（例如 `make** [PR6657]
- **在判断是否允许 DP padding 等运行时行为时，应使用运行时确定的变量（如函数参数 `cudagraph_mode`），而非静态配置（如 `self.co** [PR7297]
- **当用户通过命令行参数（如 `--block-size`）显式指定配置时，后端逻辑应优先保留用户指定的值，不应被平台默认逻辑覆盖。检查顺序应为：用户指定 > 模型** [PR7528]
- **获取字典值时应使用 `.get(key, default)` 提供默认值，避免多层嵌套判断；方法调用应选择最简洁直观的形式。** [PR7546]
- **在 Python 类中，可变的配置属性（如 `reorder_batch_threshold`）应作为实例属性在 `__init__` 中初始化，而不是作为类属** [PR7586]
- **当父类在 `__init__` 中创建了依赖某些字段的组件（如 runner），而子类在后续才设置这些字段时，子类必须重新初始化该组件，以确保组件持有正确的字段** [PR7674]
- **使用 `%` 格式化字符串时，多个参数必须用元组包裹，否则会导致语法错误或输出原始格式化字符串。** [PR9731]
- **当配置项（如 `enable_block_verify`）控制某个功能的启用时，应在运行时添加明确的保护条件，确保功能仅在满足前提条件时生效。** [PR9772]
- **在 vllm-ascend 项目中，vLLM 相关的环境变量应通过 `envs_vllm` 统一管理，而非直接使用 `os.environ` 设置，以确保环境变** [PR9935]
- **部署教程中不应使用具体的文件路径（如 `/root/.cache/`），应使用通用占位符或变量，因为路径与用户配置强相关。** [PR10148]
- **模型部署文档应严格遵循项目模板：章节标题使用 "Performance Evaluation"、删除模板中的 "Optional" 等写作指令、使用 tab-s** [PR10728]
- **在部署教程文档中，当配置项（如 `fuse_muls_add`）存在默认值时，应明确说明其默认行为。如果默认值已经是 `true`，则不需要在配置中显式设置，或** [PR11640]
- **修改教程文档中的配置命令时，应检查所有适用的硬件系列（如 A2、A3、A5），确保修改覆盖完整，避免遗漏某个场景。** [PR12048]
- **更新模型教程中的 speculative decoding 配置时，应确保所有硬件系列（A2、A3 等）的配置示例都被同步更新，避免遗漏某个系列。** [PR12144]
- **文档/脚本中同一环境变量不应被重复 export，冗余的设置会造成混淆且可能覆盖正确的值。应只保留一次设置。** [PR12491]
- **文档/教程中的多行 shell 命令，最后一行参数末尾不应有续行符 `\`，否则会转义换行符导致用户复制运行时出现语法错误。** [PR12491]
- **当部署文档多次重复展示同一个参数（如 `--max-model-len`）时，所有出现位置（CLI 命令、参数说明表、不同场景的对比表）必须使用完全相同的数值；** [PR13143]
- **当表格某一列表示"特定条件下的值"时，列名本身要把最关键的前置条件（节点数、量化方式、硬件型号等）写进去，避免只写参数后缀，导致读者跳读或打印后信息丢失。** [PR13143]
- **部署教程文档中的环境变量和启动参数应只保留必要项，移除与该部署场景无关或可能误导用户的配置（如默认值的显式重写、与该模型无关的环境变量）。** [PR14325]
- **多节点部署教程应区分 host network 和 bridge network 模式，避免在 host network 模式下展示无意义的端口映射。同时应提供** [PR3275]
- ****检查点**: 教程中提供的启动命令和参数（如 max-model-len、rope-scaling）必须在对应版本上实际运行验证** [PR4358]
- **教程文档应准确描述模型支持状态，部署命令应使用标准用法，不应包含已默认开启的环境变量或不必要的配置。** [PR4636]
- **文档中的示例代码必须是可运行的，环境变量键名等必须使用字符串字面量而非未定义的变量名。** [PR4957]
- **文档中不应删除用户获取模型的路径信息（如 modelscope 链接），已默认启用的功能无需在示例中手动设置，环境变量（如 `OMP_NUM_THREADS`）** [PR5426]
- **文档示例中不应使用已废弃（deprecated）的参数或配置项。当参数被标记为废弃时，应及时更新文档使用新的替代参数。** [PR6476]
- **在自动化 skill 文档中引用的 GitHub 操作方式（如 label、milestone 等）必须与项目实际使用的工作流一致，不能使用不存在的标签或过时的** [PR6971]
- **是由于单节点会又在一个yaml中包含多个测试用例的场景，所以才有case_index，多节点没有这种场景，所以多节点不需要case_index吗“”** [PR8368]
- **模型测试配置中的 `max_model_len` 参数需要根据模型实际支持的序列长度设置，过小的值会导致 nightly 测试用例失败。** [PR10485]
- **测试配置文件中不应包含不必要的参数；配置文件应放在正确的分类目录下；新增模型需更新 model_dataset_list.json；新增测试需说明理由。** [PR10685]
- **CI 工作流 YAML 文件中的分支引用应统一为 `main`，且同类配置（如 weekly-a2 和 weekly-a3）应保持格式一致，避免配置漂移。** [PR10974]
- **CI 辅助工具的环境变量应直接在工具脚本中使用 `os.environ` 获取，不应添加到 vllm-ascend 核心 `envs.py` 中，避免污染核心模** [PR10976]
- **新增的 e2e 测试配置文件必须注册到 nightly_config.yaml 中，新模型必须添加到 model_dataset_list.json 以便 CI** [PR11404]
- **在 PR 中不应修改与本次变更无关的 CI 配置文件，尤其是 `.github/workflows/configs/` 下的配置文件。如果修改不是功能所必需的，** [PR11489]
- **不要在代码库中保留注释掉的旧实现。注释掉的代码会增加阅读负担、造成混淆，且无法被测试覆盖。旧代码应通过版本控制追溯，而非以注释形式保留在源文件中。** [PR12772]
- **在 YAML 配置文件中以内嵌字符串形式编写的 JSON（如 `--speculative_config`、`--compilation-config` 参数值** [PR13908]
- **CI workflow 中应使用轻量级安装方式，避免不必要的 extra index 和冗余配置，优先使用 arm 架构 runner 以减少空间占用。** [PR4728]
- **CI 配置中的环境变量（如离线模式设置）应在整个仓库中保持一致，统一使用 `HF_HUB_OFFLINE` 而非其他变体。** [PR5187]
- **CI 测试配置中的 max-model-len 等参数应根据实际测试需求设置合理值，避免过大导致资源浪费或过小导致测试不充分。冗余参数应及时清理。** [PR6378]
- **Issue 自动标签系统中的 regex 模式必须避免不同标签之间的匹配冲突。当子类别（如 kimi-k2.5）的 issue 也会被父类别（如 kimi-k2** [PR7208]
- **GitHub Actions workflow 配置应遵循最小权限原则，只保留当前需要的权限；stale issue 等自动化提示文案应清晰准确，告知用户具体操** [PR7354]
- **CI 中的 issue labeler 配置应覆盖仓库中使用的所有类型前缀（包括 `[Misc]`），确保所有 issue 都能被正确分类。** [PR7531]
- **当多个 CI 工作流 job 执行相同的步骤但使用不同的参数（如不同的 Docker image）时，应使用 GitHub Actions 的 matrix 策** [PR8333]
- **plz add comment to describe the reason of setting this source separately** [PR9469]
- **当流水线同步路径中转的单元（如 V）在该代码段无任何指令执行时，应使用直连同步（如 `MTE2 -> MTE3`）而非经中转单元（`MTE2 -> V -> M** [PR12850]
- **由模型配置决定且运行期间不变的参数（如 `topk`）应在 Triton kernel 中保持 `tl.constexpr` 声明，不应为了减少重编译而改为普通** [PR13532]
- **在 `load_weights` 或其他加载期逻辑中，若只需检查某个子模块（如 `mm_projector.rot_proj`）的参数是否被加载，禁止用 `se** [PR14231]
- ****检查点**: 新增的性能优化开关应添加到 `ascend_config` 中作为 `additional_config` 的一部分，而非使用独立的环境变量** [PR4501]
- **在重构注意力机制相关代码时，条件分支逻辑应确保覆盖所有适用场景（如 MLAPO 和非 MLAPO），避免因为重构而遗漏某些特殊 case 的执行路径。** [PR6874]
- **Triton kernel 中不应将可能变化的参数标记为 `tl.constexpr`，因为每次值变化都会触发重编译；对于 batch 和 seq_len 两个** [PR7569]
- **在 sleep mode 实现中，ACL graph 参数清理应使用统一的属性列表循环处理，避免重复代码；同时需确保 attention workspace、r** [PR9585]
- **`__init__` 中根据配置（如 `hf_config.*`、`speculative_config.*`）设置 `self.xxx` 派生字段的 if/e** [PR12262]
- **不应通过 monkey-patch 修改上游库的枚举类型（如 RequestStatus）来添加自定义状态。应寻找替代方案（如使用内部状态字段或配置标志），避免** [PR12292]
- **共享代码路径中不应硬编码特定模型的常量（如 block_size），应从模型配置中动态读取，使不同模型可以保留各自的训练参数。** [PR12950]
- **当某段对局部数据的条件覆盖逻辑（如 `if is_noncausal:` 改写 `local_seq_lens`）与该数据的构建方法职责重叠时，应将分支下沉到构** [PR13249]
- **vLLM CLI 参数使用连字符（kebab-case），配置文件中引用 CLI 参数时必须与实际参数名完全一致，避免使用下划线导致参数无法识别。** [PR13285]
- **E2E 测试 YAML 配置中的 vLLM CLI 参数（如 `--max-num-seqs`、`--max-num-batched-tokens`、`--sp** [PR13914]
- **新增功能若同时适配 MRV1 和 MRV2，参数化测试矩阵（`use_v2=True/False`）必须确保 v1（`use_v2=False`）分支被测试覆盖** [PR13975]
- **当用户显式通过配置/environment variable/API flag 打开某项非默认能力（例如 `batch_invariant_mode=True`** [PR6590]

## 文档与注释 (75 条)

- **项目代码中的所有注释必须使用英文编写，禁止使用中文或其他非英文语言，以确保国际化团队协作的可读性。** [PR10627, PR10980, PR3804 等9个]
- **代码中使用的非显而易见的数值常量（如 blk_size=8192）必须添加注释说明其含义、来源和选择原因，特别是当该值与上游项目不同时。** [PR10087]
- **Python 模块导入时不应触发文件系统操作（如 `os.makedirs`）或注册 handler。这些操作应在显式的初始化函数中执行。对于循环依赖问题，延迟** [PR10306]
- **当配置字段从上游（如 `speculative_config`）获取值时，字段命名应准确反映其含义（block 数 vs token 数），避免命名误导。如果命** [PR10565]
- **代码中的 magic number（如 `4`）应定义为具有描述性名称的全局常量（如 `MAX_CAPTURE_SIZES_FOR_950 = 4`），以提高代** [PR10567]
- **当代码中存在关键的不变量假设（如 `real_num_tokens <= qkv_fla.shape[0]`）时，应添加断言或至少添加注释说明该假设成立的原因，** [PR10596]
- **在量化方法实现中，涉及位重解释（bit reinterpretation）、类型转换等非直觉操作时，必须添加注释说明为什么需要这样处理，以便后续维护者理解。** [PR10609]
- **新增的功能代码应包含清晰的注释说明设计意图，函数命名应遵循项目已有的命名风格（如 use_xxx），重复的工具函数应合并而非独立存在。** [PR10643]
- **代码中的注释、辅助代码段应定期清理，确保不包含无效或过时的内容。重构时应检查并移除不再适用的注释和代码。** [PR11081]
- **warmup 后的内存日志应当：(1) 使用统一的描述格式（"torch peak allocated memory"）与前文保持一致；(2) 不应保留含义重复** [PR11495]
- **当重命名函数参数或变量时，必须同步更新函数体内所有引用该名称的位置，否则会导致 `NameError` 等运行时错误。特别是当移除局部变量定义、改为参数传入时，** [PR12536]
- **dataclass 字段的 docstring 必须放在字段定义行的正下方，使用三引号格式，而不是使用行内注释。所有 dataclass 字段都应有 docst** [PR2913]
- ****检查点**: 函数名中包含的修饰词（如 `disable_pad`、`enable_pad`）应与其实际行为一致，避免因缩写或截断导致语义混淆。; 当函数名** [PR3892]
- **在 Triton 内核实现中，BLOCK_SIZE 等性能相关参数不应使用随意值，应通过性能基准测试调优，并在代码注释中说明选择依据。** [PR4119]
- **代码注释中使用的缩写（如 PCP、DCP）必须在首次出现时展开全称，确保所有开发者都能理解其含义。** [PR4143]
- ****检查点**: 功能指南文档中，安装步骤应在使用说明之前** [PR4526]
- **当 if/elif 语句中包含三个或更多条件时，应将这些条件抽象为独立的函数，以提高代码可读性和可维护性。** [PR5154]
- **对核心模块进行 monkey-patch 时，必须在补丁文件中详细描述补丁的原因、计划和已知问题，以便后续维护者理解补丁的背景和必要性。** [PR5308]
- **回滚某段实现（如从 Triton kernel 回滚到 PyTorch）时，应保留原始代码并添加 TODO 注释说明回滚原因和后续复用计划，而非直接删除。** [PR5391]
- **Please add comments to explain why this is needed.** [PR6263]
- **在判断 tensor 维度时，应使用最简洁且充分的条件（如 `dim() == 2`），避免添加不必要的额外检查条件，以提高代码可读性和可维护性。** [PR7010]
- **当上游已有类似功能实现时，应优先复用。如果因平台差异需要自定义实现，必须在代码中添加注释说明与上游实现的差异及原因。同时，当上游提供了新的扩展机制（如 plat** [PR7044]
- **在 MoE 通信实现中，当不同分支对数据类型（如 nz format）或参数传递方式（如空 tensor vs None）有特殊要求时，必须添加注释或 TODO** [PR7066]
- **在 310P 平台特定的代码中，当行为与 910B 不同时，必须添加注释说明差异原因。上下文管理器等辅助工具的命名应使用动宾短语，体现其实际用途。变量名必须与实** [PR7134]
- **Let's merge these branches into one, and make comment clearly.** [PR8265]
- **Remove comment code** [PR9035]
- **所有临时性 patch 代码必须在代码注释中明确描述移除计划和条件，以便后续维护者了解何时可以安全移除该 patch。** [PR9706]
- **涉及 meta tensor 判断和处理的代码应添加注释说明 meta tensor 的含义（仅保存 shape/dtype 元信息，无物理内存），以及为何不需** [PR9854]
- **在描述量化方法的文档中，必须明确标注每种量化方法支持的硬件类型，避免用户在不支持的硬件上使用。** [PR10332]
- **项目文档中关于 prefill 阶段的描述应统一使用 "prefill" 写法，不使用 "pre-fill"、"pre fill" 等其他变体，以保持文档术语的** [PR10588]
- **文档中所有章节标题必须使用统一的编号格式（如 "1. xxx", "2. xxx"），确保文档结构清晰一致。** [PR11085]
- **Markdown 文档的一级标题必须与文件名对应（如 `Qwen-VL-Dense.md` 的标题应为 "Qwen-VL-Dense"）。** [PR11138]
- **当文档中需要展示多个平台/操作系统的配置命令时，应使用 Sphinx 的 `tab-set` 标记语法将不同平台的内容分组到标签页中，提高可读性和可维护性。同时** [PR11179]
- **模型部署教程文档应严格遵循模板格式，包括：统一的章节标题命名、正确的语法（单复数等）、每个章节开头添加模板要求的介绍注释。** [PR11335]
- **当代码实现依赖特定外部技术/算法（如 QuaRot 旋转量化、Rope Scaling 等）时，应在函数或关键逻辑上方加注释，指明技术名称与用途，降低后续维护者** [PR12262]
- **文档文件应通过 markdown lint 检查，不应存在格式异常或"看起来奇怪"的内容。修改文档时应确保整体格式一致性。** [PR12308]
- **在部署/启动文档中，"参数说明"段对每个参数取值的描述必须与同节"示例启动命令"中实际使用的取值完全一致。当从其他硬件系列文档复制粘贴时，必须同步修正 TP/D** [PR12426]
- **官方教程文档中提供的 Docker 镜像地址必须是官方仓库维护的镜像，不能使用个人或第三方私有镜像，以保证镜像的可获得性、安全性和持续维护。** [PR12491]
- **部署文档中的 docker 镜像标签、版本号等引用不得残留 `#TODO` 等占位符；模板变量必须使用项目统一的 Jinja 语法 `{{ var }}`，不得** [PR12718]
- **文档英文内容须符合基础语法（如助动词 does/does not 后使用动词原形，而非 -s 第三人称形式）与标点间距规范（括号前留空格、逗号/句号后留空格），** [PR13003]
- ****检查点**: 功能描述中使用的术语应准确反映其含义（如 "KV Transfer" 比 "management" 更精确）。; 同一文档中同一概念的命名应保** [PR3950]
- **教程文档中应引用 `examples/` 目录中的脚本，而非将所有代码直接内联到文档中，以避免代码重复和维护不一致。** [PR4182]
- **使用特定 Python 版本语法特性的示例代码和文档，必须在显著位置标注最低 Python 版本要求。** [PR4265]
- **教程文档中的产品名称、术语必须保持全局一致，包括大小写、标题格式、参数命名等。** [PR4543]
- **更新文档中的下载 URL 时，必须同步更新文档中所有引用该资源的名称/路径，确保 URL、模型名称、示例命令三者一致。** [PR5238]
- **文档中的性能数据应使用标准基准测试工具（如 aisbench）而非手动测试数据，功能特性说明应标注最低支持的版本号。** [PR5373]
- **当文档涉及设备内存、网络带宽等资源消耗时，应提供具体的计算公式或量化说明，帮助用户评估资源需求。** [PR6126]
- **当前 vllm-ascend 文档栈是 **Sphinx（构建器）+ MyST Parser（markdown 引擎）+ ReadTheDocs（托管）**，不** [PR6910]
- **发布说明中每个条目必须附带对应的 PR 链接。不应在通用发布说明中包含特定硬件（如 950）或特定平台（如 310p）的进度信息。已知问题需提供完整的描述和可用** [PR7067]
- **在进行文档链接更新和格式修复时，应注意不要修改特定的配置内容或安装说明，这些内容可能有特殊的用途或依赖关系。** [PR7734]
- **文档示例中应使用占位符（如 `xx.xx.xx.xx`）替代真实 IP 地址，避免信息泄露。环境变量名称应仔细检查拼写。对于已知不支持的功能，应添加明确的说明。** [PR7866]
- **Markdown 文档中的行尾不应有多余的尾部空格，这会影响代码审查的 diff 显示质量，也可能导致 lint 检查失败。** [PR7920]
- **当某个功能仅在特定部署模式或特定节点类型下可用时，必须在所有相关文档中明确说明限制条件，并在代码中添加相应的验证逻辑。多个文档涉及同一功能时需保持一致性。** [PR8143]
- **发布说明中的已知问题应仅列出经过确认的实际问题，而非所有提出的 issue；内容应按照正确的章节组织（如依赖相关内容放在 dependency 章节）；发布日期** [PR9602]
- **所有公开函数的定义处应添加 docstring，包含参数说明（Args）和功能描述，帮助后续维护者理解函数用途。** [PR9900]
- **发布说明文档中的版本兼容性矩阵应包含所有关键依赖的版本信息（如 Mooncake、HDK、CANN 等），并确保日期与实际发布日期一致。** [PR9968]
- **CI workflow 文件中的注释应简洁明了，调试性或临时性注释不应留在最终代码中。** [PR10701]
- **将测试用例从 nightly 迁移到 weekly 工作流后，上传 artifact 的 `name` 前缀须同步从 `nightly-*` 改为 `weekl** [PR10967]
- **PR diff 中不得提交/保留形如 `# # 旧代码说明` 或被整块 `if False:` 的死代码。历史代码在重构后如果迁移到了别处或逻辑失效，应从源代码** [PR11925]
- **在 `.github/workflows/` 目录中，以 `_` 前缀命名的 workflow 文件不会被 GitHub Actions 自动触发。手动触发的 ** [PR12140]
- **当代码中出现非直观的 early-return（尤其是与上游实现行为不一致、或不同代码路径有差异化处理时），必须在 early-return 处添加注释，说明为** [PR12997]
- **当 `if config.mode == "A": ... else: ...` 这类分支的语义不是字面上的"切换模式"，而是映射到更具体的业务行为（如 `al** [PR14254]
- ****检查点**: Dockerfile 和 CI 配置文件名应清晰反映其用途，避免过于简略的命名。; 文件命名应遵循项目已有的命名约定。** [PR3989]
- **Using `bash .sh` in a Dockerfile is not a good idea. For clarity, please write t** [PR4409]
- **CI 镜像构建 workflow 中的关键配置（如缓存 registry 选择、repo 路径、branch 信息等）必须添加注释说明其用途和选择原因。缓存 r** [PR7040]
- **使用 torch_npu 新版本接口时，应添加版本兼容性检查和 TODO 注释，说明当前支持的版本范围。如果新版本接口性能更优，应评估是否可以在满足版本条件时默** [PR11375]
- **修改张量的 dtype（如从 int64 改为 int32）时，应在代码注释或 PR 描述中说明原因，确保评审者理解变更的必要性。** [PR4113]
- **当为了性能优化目的将代码逻辑封装为独立方法或算子时，必须添加注释说明封装的原因、性能收益数据，以及为何不影响正确性。** [PR4830]
- **当从 vllm 上游复制代码并进行 Ascend 特定修改时，必须在代码中添加注释说明与上游的差异及修改原因，以便后续维护者理解修改背景。** [PR7221]
- **当多个 if 条件语句处理逻辑存在重叠或可以合并时，应将条件合并为一个统一的判断，减少代码冗余，提高可读性和可维护性。** [PR8043]
- **当 Triton kernel 的 autotune 参数（如 BLOCK_SIZE）受到限制以防止不同芯片上的 UB（未定义行为）时，应添加 TODO 注释说** [PR9830]
- **当代码中出现非直觉的 tensor 操作（如拼接零值、填充等）时，应添加注释说明操作的目的和原因，提高代码可读性。** [PR9892]
- **单元测试应仅验证被测代码的实际行为，不应包含对 pass-through 字段（仅原样传递未做处理的字段）的断言，测试意图应通过命名和注释清晰表达。** [PR12950]
- **Triton kernel 及其变量的命名应准确反映其实际执行模式（如并行/串行）和语义概念（如 position vs slot、tile size vs b** [PR13191]
- **当布尔表达式作为参数传入且语义不直观时（尤其是反向比较如 `== 0`），应提取为正向命名的布尔变量（如 `has_prefill`），再用 `not` 表达反** [PR13249]

## 代码规范与风格 (66 条)

- **平台特定的常量（如 ASCEND950 的 workspace 大小参数）应从分支逻辑中提取出来，集中定义在统一的常量块中，并使用平台前缀的大写命名（如 `AS** [PR10041]
- **新增激活函数时，需要确认是否与已有的类似实现（如 `AscendSiluAndMulWithClamp`）存在差异，明确数学语义区别（如 clamp 的位置），** [PR10491]
- **在 vllm-ascend 插件中实现新功能时，应优先继承 vllm 已有的基类和方法，通过 `super()` 调用父类方法后添加 Ascend 特有逻辑，避** [PR10592]
- **在移除旧版本兼容代码时，应逐一确认每个被删除的函数、环境变量和 patch 是否仍被其他代码引用，避免误删导致功能回归。** [PR10704]
- **当硬件平台（如 A5）的算子支持发生变化（如 rmsnorm 不再需要 bias）时，应及时删除相关的旧模式类，避免代码冗余和潜在的误用。** [PR10730]
- **当多个文件中存在几乎相同的内存管理逻辑（如 sleep mode 标记处理）时，应提取为公共 helper 函数，避免维护成本增加和逻辑不一致。** [PR10775]
- **当修复 eager 导入导致的依赖问题时，延迟加载应仅应用于实际有问题的模块（如 NPUIPC 依赖 ray），而不应将无问题的模块（如 HCCL）也改为延迟加** [PR10816]
- **升级 vLLM 版本时，应检查现有 patch 是否仍需要，统一方法签名风格（使用具名参数替代可变参数列表），并在关键路径添加 assert 校验确保数据完整性** [PR10935]
- **当使用 `super(ParentClass, self)` 跳过父类直接调用祖父类方法时，必须添加详细注释说明原因，避免后续维护者困惑。** [PR11461]
- **在 vLLM-Ascend 的 platform patch 机制中，`adapt_patch` 通过 Python 模块缓存只导入一次。因此不需要使用 `_P** [PR11589]
- **在类的 `__init__` 方法中，不应对同一实例变量进行两次赋值。如果需要对变量做类型转换，应使用 `.to()` 等方法进行转换而非重新赋值。** [PR11602]
- **在 speculative decoding 的实现中，常量命名应遵循 vllm 上游的命名规范（如 `SAMPLE_FROM_ANCHOR`）。同时，关键方法** [PR11765]
- **在修复 bug 或重构代码时，应检查并移除不再有用的代码行，避免遗留无用逻辑增加维护负担和混淆。** [PR11807]
- **在 speculative decoding 中启用特定功能（如 KV sliding window）时，应检查 drafter 的 backend 类型是否兼** [PR12103]
- **仅作为内部使用的接口/方法应使用下划线 `_` 前缀命名，以符合 Python 的命名约定，明确表示该接口不应用于外部调用。** [PR12336]
- **在 MoE/shared expert 的 token 分片逻辑中，按 `tp_rank` 从分片结果取对应 rank 的 chunk 时，必须用 `torch** [PR13158]
- **对于 `state[block, dim, state_len]` 这类以 `state_len` 为滑动轴的 DS 布局逐行拷贝，循环次数必须是行数 `dim** [PR13477]
- **从 Triton kernel 模块导入函数时，必须使用 `HAS_TRITON` 进行条件导入，否则在 Triton 不可用的环境（如 CPU/CANN fa** [PR13509]
- **在 Triton kernel 中使用 inline `if-else` 表达式时，两个分支都会被求值。如果某个分支引用了在当前条件下未定义的变量（如 RMS ** [PR13509]
- **在 Python 中通过关键字参数（kwargs）调用函数时，kwarg 名称必须与函数定义中的参数名完全一致，否则会抛出 `TypeError`。特别是当引入** [PR13819]
- **当类的 `__init__` 通过 `kwargs.get("xxx")` 取值（默认 `None`），但该属性在后续方法中被严格依赖（例如 `_get_lay** [PR14375]
- **当函数需要返回多个相关值时，应使用 dataclass 或 namedtuple 封装返回值，而非返回裸 tuple，以防止数据传递过程中的顺序错误和类型混淆。** [PR1995]
- **在进行模型重构时，应删除不再需要的冗余文件，并确保相关的端到端测试被重新启用，以验证重构后的功能正确性。** [PR2437]
- **在工具函数中，当参数可能为 None 时，应仅在需要时才执行 import 操作，既减少不必要的导入开销，又提高函数的健壮性。** [PR3238]
- **新增功能（如 chunkprefill 支持 PCP/DCP）时，不应修改与功能无关的代码。应删除多余注释、未使用的函数和不可达分支，保持代码整洁。typehi** [PR3801]
- **在 dataclass 或类似的数据结构定义中，每个字段之间必须正确使用逗号分隔，避免因语法错误导致运行时异常。** [PR4183]
- **函数参数的类型注解必须准确反映实际传入的数据结构，如 `list[torch.Tensor]` 不应标注为 `torch.Tensor`。** [PR4216]
- **配置项的命名应准确反映其用途和类型，如 `dump_path` 比 `dump` 更明确地表示这是一个路径值，而非布尔开关。** [PR4241]
- ****检查点**: 对上游 vllm 接口（如 KV Connector base class）的封装类，其 `__init__` 方法参数必须与上游保持一致** [PR4411]
- ****检查点**: 移除算子分支时，应检查所有相关变量（如 fia_mask）是否也可以一并清理** [PR4531]
- **当需要替换 FusedMoE 的整个 forward 流程时，应使用明确的方法名（如 `forward_oot` 或 `forward`），而不是通过重命名内部** [PR4562]
- **自定义算子的 C++ 内核代码应包含充分的注释，避免命名空间污染，成员变量遵循小写+下划线命名规范。** [PR4606]
- **在子类或模块中定义属性前，应检查父类（如 gpu model runner）是否已经初始化了该属性，避免冗余定义导致维护混乱。** [PR4973]
- **重构过程中应确保特定功能的代码被移至对应模块，原模块中不应残留已迁移功能的代码。** [PR5097]
- **在 speculative decoding 中，graph capturing 的条件判断应与已有实现（如 propose 方法）保持一致，参考 PR #50** [PR5118]
- **移除某个功能时，应彻底清理所有相关代码，包括关联的 class、引用和测试，避免残留废弃代码。** [PR5130]
- **环境变量应使用正确的类型（bool 而非字符串），补丁导入应在满足特定条件（环境变量设置 + 版本匹配）时才执行，避免不必要的副作用。** [PR5212]
- **当 vLLM 上游已定义了常量（如 `GREEDY_TEMPERATURE`）时，应直接从上游导入使用，而非在 vllm-ascend 中本地定义，以避免值不一** [PR5417]
- **变量名应清晰反映其语义含义和与其他变量的关系。例如，当 ssm 与 k 对齐时，变量名应体现这种对齐关系。** [PR6887]
- **在 MoE 模块中，应使用类型化的数据类（如 dataclass）封装函数间传递的参数，避免使用长参数列表。当存在功能重叠的类时应合并，并优先继承已有的基类（如** [PR7024]
- **公共静态方法/实例方法名如果把 `attribute` 相关缩写拼写错（常见 atrr / atrrib / attib / att rr 把 t/r 写两次）** [PR7110]
- **代码中不应直接使用魔法数字（如 `128*128`），应使用命名常量（如 `ATTENTION_BLOCK_SIZE_LIMIT=128*128`）来提高代码可** [PR7372]
- **代码中不应存在永远无法满足的条件判断，这类死代码会增加理解成本并可能导致维护问题。发现后应立即删除或修复。** [PR7717]
- **新增的代码格式（如函数签名风格）应与项目主线保持一致，以提高代码可读性和维护性。同时，修改共享配置时应确认不影响其他模型。** [PR7725]
- **代码中的经验值（如 `8`、`5`、`30`）应使用有意义的常量名替代，重复的后处理逻辑应提取为独立函数，以提高代码可读性和可维护性。** [PR7896]
- **在分布式推理场景中，函数和变量的命名应准确反映其涉及的核心概念（如 pooling、PD-mix 等），使代码阅读者能够从命名直接理解函数的用途和适用场景。** [PR8290]
- **当同一函数或关联函数中存在相同条件的多个分支时，应合并为一个分支以减少代码重复和提高可维护性。** [PR9560]
- **代码中不应直接使用未解释的数字字面量（魔法数字），应将其定义为有意义的命名常量，提高代码可读性和可维护性。** [PR9789]
- **Release notes 应完整列出该版本中所有已集成并通过测试的功能，不应遗漏已合入的代码。对于并行推测等分类下的多种方法，应逐一列出。** [PR10600]
- **发布说明（Release Notes）应包含清晰的分类结构（Deprecation & Breaking Changes、Dependencies、Featur** [PR5334]
- **代码中不应直接使用含义不明确的数值字面量（尤其是边界值、极限值），应将其命名为具有语义的常量，使代码意图自解释。** [PR13048]
- **通信优化特性（如 FlashComm2）应仅在适用的推理阶段（如 prefill）启用，避免在 decode 场景误启用导致错误。配置校验逻辑应放在合理的位置，** [PR3232]
- **在同一文件中的注释格式应保持统一一致，包括注释风格、缩进、标记方式等。性能优化重构时尤其需要注意注释规范的一致性。** [PR4944]
- **子类的 `__init__` 方法应调用 `super().__init__()` 来初始化父类状态，避免手动复制父类初始化逻辑导致的代码重复和维护问题。** [PR12292]
- **当上游将某类重构/重命名（如 `AscendFusedMoE` → `AscendMoERunner`，`FusedMoE` 由类变为工厂函数返回 runner** [PR12885]
- **当测试通过 monkeypatch 修改了全局/类属性，并依赖 `finally` 还原时，所有可能抛异常的步骤（尤其是执行外部代码的 `runpy.run_p** [PR13083]
- **不要通过 `get_forward_context().xxx = value` 在标准 vLLM `ForwardContext` 对象上动态设置未在类定义中** [PR13155]
- **调用函数时只传入该函数实际使用的参数，不应传递函数体内部并不使用的冗余参数；冗余参数会让调用点产生"该参数会影响函数行为"的错觉，增加误读风险。** [PR13155]
- **当一个 Speculator 子类（如 `AscendDFlashSpeculator`）通过方法注释声明"mirroring"另一个 Speculator（如** [PR13470]
- **当项目已为量化方法名（如 `"fp8"`）定义了全局常量（如 `FP8_METHOD`）时，`@register_scheme` 装饰器及其他引用该方法名的位置** [PR13651]
- **当新增配置项的合法性校验（如设备类型限制、数值上限、互斥配置）时，校验逻辑应作为对应配置类的 `_validate_*` 私有方法实现，并在该配置类 `__in** [PR13790]
- **当重构将多个方法统一为单一方法后，必须同步移除或更新调用旧方法名的测试，否则会导致 `AttributeError` 运行时失败。** [PR13995]
- **每个测试用例都应有明确的验证目标。如果测试用例没有验证任何实质内容，或者多个测试用例验证相同逻辑，应删除冗余测试。** [PR14023]
- **当多个返回值结构存在公共字段时，应使用 Python dataclass 替代 NamedTuple，并提取公共基类以减少代码重复，提高可维护性。** [PR2465]
- **工具函数应放在与其功能最紧密相关的模块中，而非通用的 utils.py。如果多个文件需要调用某个功能，应将其放在对应功能模块的 utils 中。** [PR4214]
- **当重构一个被多处引用的核心 Python 模块文件名时（如 `worker_v1.py` → `worker.py`），仅做本体文件 rename 是不够的，必** [PR5051]

## 硬件适配 (57 条)

- **当代码中为了避免 D2H（Device-to-Host）传输而重新计算 CPU 端 tensor 时，应添加注释说明原因和哪些下游操作依赖这些 CPU tens** [PR10023]
- **在构建 KV cache 的 layer-to-group 映射时，必须正确处理 Pipeline Parallel (PP) 和 Multi-Token Pr** [PR10202]
- **在支持图模式（graph mode）的硬件架构上，代码路径中不应存在 CPU 同步操作（如 `.cpu()`、`.item()`），否则会导致图捕获失败或性能下** [PR10309]
- **在 NPU 内存受限场景下，`.contiguous()` 会物化张量的完整副本。如果后续操作能处理非连续输入（如 `maybe_trans_nz()`），应移** [PR10454]
- **在多并行（DP/TP/PP/CP）环境下，SSD offload 路径必须使用全局唯一的 rank 标识（如 `torch.distributed.get_ra** [PR10477]
- **`.item()` 会触发 device-to-host 同步操作，在模型前向传播的热路径中会严重阻塞 CPU，降低性能。应使用等价的整数变量替代。** [PR10487]
- **MoE（Mixture of Experts）相关的操作逻辑（如 grouped matmul、swiglu 激活等）应统一放在 `vllm_ascend/op** [PR10556]
- **当使用 Eagle3 speculative decoding 配合 Chunked Pipeline Parallelism 时，如果 `profile_cp** [PR10566]
- **在 vllm-ascend 项目中新增 patch 文件时，必须在 `vllm_ascend/patch/__init__.py` 中声明该 patch 的 m** [PR10694]
- **涉及复杂场景（如 PP+MTP+PD 分离）的 BugFix PR 必须在 description 中说明：问题根因、修复逻辑、影响范围。同时应补充必要的单元测** [PR10855]
- **在 `vllm_ascend/patch/__init__.py` 中，每个 patch 段都有编号索引。当移除某个 patch 后，必须重新编号后续段落以保持** [PR11053]
- **在 Ascend NPU 上，权重转换为 NZ（Non-Zero contiguous）格式后，不应再调用 `.contiguous()` 操作。NZ 格式使用** [PR11068]
- **注意力模块中的设备特定逻辑（如 A2/A3/A5 不同的处理方式）应统一放在 `DeviceAdaptor` 中，不应散落在 `utils.py` 等通用模块中** [PR11091]
- **特定算子（如 SFA C8）的逻辑应集中在其专用模块（如 `sfa_v1.py`）中实现，不应修改公共的 Linear apply 方法。当两个算子绑定时（如 ** [PR11228]
- **在 vllm-ascend 中实现的 vLLM 模块功能应使用 `vllm_` 前缀命名以标识其来源。代码注释应至少与上游 vLLM 侧等量。Ascend NP** [PR11267]
- **在 Prefill Context Parallelism (PCP) 中，KV cache interleave size 可能大于 1。MTP attent** [PR11492]
- **在替换 PA KV cache 操作符时，应统一使用新的 `torch_npu.npu_scatter_pa_kv_cache` 和 `torch_npu.np** [PR11713]
- **当将 Triton kernel 的 grid 从固定值 `[1,]` 改为动态值 `[batch_size,]` 以并行化时，必须用 `if batch_si** [PR14066]
- **在实现分布式功能（如 CPU offload connector）时，必须考虑所有并行维度（TP、PP、DP），不能只关注部分维度。** [PR1659]
- **在分布式环境中创建 tensor 时，应明确获取当前设备而非依赖 `set_device` 的隐式行为，避免因设备设置不当导致的跨 rank 错误。** [PR2704]
- ****检查点**: 新引入的融合算子（如 bmm_transpose）在未经充分验证的场景（如 tp_size > 1）中不应直接使用。; 应通过条件分支限制新算** [PR3990]
- **类名应清晰表达其职责，避免使用过于模糊的名称如 `Adaptor`，应使用更具体的名称如 `AscendCompiler`。** [PR4168]
- **在分布式场景中，应优先复用已有的通信组（如 WORLD），而非创建新的通信组，特别是在涉及 PP 的场景中，新建通信组可能导致通信域不一致。** [PR4188]
- **当某个功能仅在特定硬件平台上可用时（如 ATB 在某些 Ascend 型号上不可用），必须通过条件判断进行隔离，避免在不支持的平台上导致启动失败。** [PR4228]
- ****检查点**: 从 torch-npu 等外部项目适配的代码文件，必须在文件头部添加版权声明（如 "Adapted from xxx.py"）** [PR4431]
- **在序列并行（sfa-cp）模式下，token 计数应使用 `num_input_tokens` 而非 `num_actual_tokens`，tensor 切片** [PR4850]
- **更新同时存在于 CPU 和 GPU 上的 tensor 时，应通过 `.np` 属性直接修改 numpy 数组以保持 `pin_memory` 的关联，避免创建** [PR5298]
- **`BaseDeviceOperator`** [PR5735]
- **maybe we shoult drop support for 310p first.** [PR6442]
- **当脚本或工具仅适用于特定硬件设备（如 310P、Atlas 300I）时，文件名应包含设备标识，以避免用户在不兼容的设备上误用。** [PR6878]
- **当新增的辅助函数（如 `get_rope_dim`）具有通用性，可能被多个模块复用时，应将其放置在公共工具模块（如 `vllm_ascend/utils.py`** [PR6880]
- **Ascend 设备上 MOE 通信方法（MC2 vs ALLGATHER）的选择需要同时满足多个条件（如 num_experts_per_device 和 ep** [PR7364]
- **在需要同时创建 CPU 和 device tensor 进行数据传输时，应使用 `CpuGpuBuffer` 统一管理；代码中的数字常量应使用命名常量并添加注释** [PR7487]
- **在 `vllm_ascend/patch/worker/` 与 `vllm_ascend/patch/worker/patch_v2/` 下所有 Triton ** [PR7578]
- **`DeviceOperator` 的设计目标是在不同硬件上支持不同的算子实现，而不是用来兼容所有硬件差异。硬件差异的兼容逻辑应放在各自的模型实现层（如 mla_** [PR7797]
- **too much code about this. Can it be placed in the AscendNgramProposerNPU class?** [PR9008]
- **`num_groups = input_size // self.group_size if input_size % self.group_size == 0** [PR9159]
- **move this to device_op** [PR9382]
- **This should be a test for the fused operator, rather than a CPU-vs-NPU test for ** [PR9458]
- **平台 patch 不应直接应用，而应根据运行时配置条件（如 pcp_size/dcp_size）决定是否启用。每个 patch 文件必须在 `vllm_asce** [PR9638]
- **当 quay 等公共镜像不支持特定硬件平台（如 Ascend950）时，文档应明确说明并提供源码编译指南及软件环境要求。** [PR11055]
- **技术文档中应使用社区通用术语表达，如 `TP sizes` 而非 `TP degrees`，保持术语一致性。** [PR5084]
- **Release notes 中描述已知问题或行为变更时，应提供具体的技术细节（如与 tp size 的关系）和数值示例（如 tp 2 时 block_size ** [PR7436]
- **This should be changed in:  https://github.com/vllm-project/vllm-ascend/blob/mai** [PR8640]
- **CI 配置文件中的分支引用、硬件配置项应与目标测试环境实际能力匹配。310p 不支持 multi_node 和 double_node 配置，不应出现在 310** [PR11541]
- **所有新增的 monkey-patch 必须在 `vllm_ascend/patch/__init__.py` 中登记，包含 Why（为什么需要 patch）、H** [PR12292]
- **reuse `get_vectorcore_num()` in https://github.com/vllm-project/vllm-ascend/blob** [PR5392]
- **当使用 RMSNorm 内核模拟 L2 范数时，由于 RMSNorm 在平方根内对维度 d 做了除法，传入的 eps 需要除以 dim（`eps / dim`）** [PR10408]
- **在异步 speculative decoding 模式下，`seq_lens_cpu` 的修正应利用已有的异步拷贝结果（`valid_sampled_token** [PR10506]
- **调用硬件特定融合算子（如 `npu_grouped_matmul_swiglu_quant`）时，必须验证参数是否满足算子的约束条件（如 `group_list** [PR2275]
- **print `src_device` and `dst_device` maybe better when check fails** [PR3433]
- **当存在可用的 NPU 融合算子（如 `npu_moe_gating_top_k`）时，应优先使用融合算子替代手写的 Python 层选择逻辑，以获得更好的性能和** [PR3833]
- **在异构计算场景中，应通过在 metadata 中预置 CPU 端数据（如 `seq_lens_cpu`）来避免频繁的 host-to-device 同步操作，而** [PR7617]
- **当需要 CPU 端的序列长度等数据时，应使用预计算的 `optimistic_seq_lens_cpu` 等字段，而不是在运行时进行 device to hos** [PR7787]
- **LGTM, should we consider also add `replica_num` here for future's sake?** [PR7820]
- **`DeviceOperator` 仅负责封装设备差异（不同 NPU 路径、operator 调用差异），不应承载业务逻辑（如 LSE 合并、shape 变换等）** [PR11870]
- **当多个 TP 通信组的创建逻辑相似时，应提取公共逻辑进行复用，避免代码重复，提高可维护性。** [PR2616]

## 错误处理与日志 (39 条)

- **在同一模块（如 Triton Ops）中，错误处理的方式应保持一致。如果某些文件使用 `logger.error` + `raise RuntimeError`** [PR10069]
- **每个新增的源文件都应在文件头部包含标准的版权信息声明。** [PR10077]
- **当使用 try-except 处理可能的异常时，不应在 except 块中仅使用 `pass` 静默吞掉错误。应改为显式的条件判断（如检查字符串长度），避免依赖** [PR10217]
- **Python logging 中应使用 `%` 格式化（如 `logger.error("msg: %s", value)`）而非 f-string（如 `lo** [PR10222]
- **调试日志（logger）在问题排查中具有重要价值，不应在修复 bug 的 PR 中随意删除。如果确实需要移除日志，应在评审中说明原因并获得同意。** [PR10276]
- **当同一计算结果在多处使用时，应提前计算并存储为变量，避免重复计算。特别是在日志记录和错误处理中，应将统计计算提前到需要使用的代码之前。** [PR10329]
- **当多个错误处理路径提供相关的用户指导时，应确保提示信息不在不同函数/常量之间重复。每个错误处理函数应负责特定的提示内容，避免用户看到重复的建议。** [PR10555]
- **日志标准化应聚焦于真正需要改进的地方，不应将不必要的修改（如 int 改 string、添加冗余日志）引入代码。日志级别应根据实际需要设置（info vs de** [PR10754]
- **在 Python 代码中应使用 `logger.warning()` 而非 `print(..., file=sys.stderr)` 输出警告信息，以保持日志** [PR10945]
- **在生产代码的调试诊断中，条件检查应使用 `if not condition: raise RuntimeError(...)` 而非裸 `assert`，确保在** [PR11123]
- **当代码要从一个"人工可能编辑过的配置载体"（MMC .conf、.properties、TOML/INI 的手工拼接、甚至 JSON/YAML）读取布尔开关/枚** [PR12064]
- **在 `@triton.jit` 装饰的 kernel 函数内部，所有合成 boolean 条件（mask 合并、条件比较结果组合、accepted/reject** [PR12256]
- **Python 中空代码块（如 `if`、`except`、`class` 等后面没有语句）必须包含 `pass` 语句，否则会导致 `IndentationEr** [PR12292]
- **当分布式通信原语（如 `dist.all_to_all_single`）通过 `tensor.view(world_size, -1, dim)` 在 dim ** [PR12923]
- **Triton 的 `tl.arange` 要求大小为 power-of-two。对于非 power-of-two 的维度（如 hidden_size=7168）** [PR13509]
- **错误信息应精确反映当前不支持的功能，而非笼统地禁止一组功能。当部分功能已支持时，应更新错误信息。** [PR14023]
- **assert 语句应提供清晰的错误描述信息，帮助开发者快速定位问题。并行组的创建应根据模型类型按需创建，避免不必要的资源消耗。** [PR1956]
- **对于用户配置校验，应使用 `raise` 抛出带有明确信息的异常，而非 `assert`。assert 可能在优化模式（-O）下被跳过，不适合作为配置校验手段。** [PR2309]
- **在提交代码之前，必须确保所有调试用的 print 语句已被移除，避免污染生产环境的日志输出。应使用 logger 替代 print 进行日志记录。** [PR2601]
- ****检查点**: 检测 Triton 等可选依赖时，应使用上游 vllm 提供的标准标志（如 `HAS_TRITON`），而非自定义 try/except** [PR4443]
- **The atb operator may report an error in A5.** [PR7222]
- **当异常被捕获且不抛出时，不应使用 `error` 级别日志。应根据实际处理行为选择合适的日志级别（如 `warning` 或 `info`），并添加注释说明为何** [PR7514]
- **当添加硬件或模型相关的约束检查时，错误消息应明确说明不支持的具体场景、原因以及已知的底层问题，便于用户定位和解决问题。** [PR7679]
- **当底层算子（如 `npu_fused_infer_attention_score_v2`）已经内部处理了某些逻辑（如 sliding window attent** [PR8387]
- **just return prompts instead for raising error?** [PR8448]
- **当需要为特定平台（如 310P）的所有日志添加统一前缀时，应使用 `LoggerAdapter` 动态注入前缀，而非在每个日志调用处手动添加（避免 shotgu** [PR9953]
- **模型文档的 FAQ 章节应：(1) 按模板格式编写；(2) 避免提供可能误杀进程的破坏性命令（如 `pkill -f vllm`），应引导用户检查并定向处理；(** [PR10884]
- **Markdown 表格的分隔符列数必须与表头和数据行的列数严格一致。列数不匹配会导致渲染异常或显示空列。** [PR11533]
- **在构建脚本中，应使用项目定义的 `log` 函数进行日志输出，而非直接使用 `echo`，以保持日志格式一致性和可追溯性。** [PR10864]
- **在生产代码中，必须使用 Python `logging` 模块（`logger.info`/`logger.debug`/`logger.warning` 等）** [PR12292]
- **在创建新文件时，必须使用项目标准的 copyright header，确保版权声明和许可证信息正确。** [PR2826]
- **每个 PR 应专注于单一目标。如果某行修改与 PR 的核心目的无关，应将其移除或在独立 PR 中处理，避免引入不相关的变更。** [PR2198]
- **在移除 `.to(dtype)` 等类型转换操作时，必须确认上下游数据的类型一致性，避免因类型不匹配导致的计算错误或运行时异常。** [PR4154]
- **Only the last handle is useful for the coming process because they are using the** [PR7029]
- **测试代码中不应使用 `try..except: pass` 来静默吞掉异常。测试应当让异常正常抛出，以便及时发现和定位问题。如果需要错误信息，应使用 `prin** [PR7757]
- **不应使用 try-catch 来探测属性是否存在或进行条件分支，应通过显式参数传递和条件判断来处理。** [PR12950]
- **在模型前向传播路径中，`get_tp_group()` 等分布式基础设施调用在引擎初始化后必然可用，不需要用 try/except 包裹做防御性处理。防御性 t** [PR13142]
- **当代码中存在复杂的状态判断逻辑时，应考虑使用 Enum 类来表示状态，提高代码可读性。同时在关键分支处添加 `logger.warning` 日志，便于问题排查** [PR2532]
- **从 `prefix` 等结构化字符串中提取信息时，不应使用硬编码的 split 索引（如 `[2]`），因为该索引假设了固定的层级结构，模型命名变化时会导致错误** [PR3833]

## 版本兼容与同步 (27 条)

- **在进行跨版本兼容性维护时，版本号判断必须准确无误。对于 Triton 编译期间的限制（如 `is_compiling`），需要明确注释说明原因。批量同步 PR ** [PR10250]
- **在版本升级同步 PR 中，应检查是否存在不必要的类型包装（wrap）和类型转换（cast）。直接使用 `isinstance` 检查和属性访问即可，无需额外包装** [PR10459]
- **在进行依赖版本升级（如 vLLM v0.22.1 → v0.23.0）时，应仅修改与版本兼容相关的代码，不应误改不相关的逻辑行。升级 PR 应最小化变更范围，避** [PR10534]
- **移除本地 monkey patch 时，必须确认上游 vLLM 对应功能已合入当前使用的版本。应在 PR 描述中明确标注对应的上游 PR 编号和版本，避免误移除** [PR10902]
- **当将元数据从 host 端迁移到 device tensor 时，应同步清理所有依赖 host 元数据的冗余代码（如 INT64 常量、不必要的 if-else** [PR11161]
- **当 main2main 升级 PR（如"upgrade vLLM commit to 0717"）同时包含 API 兼容变更、hash 算法迁移、补丁层重构时，** [PR12502]
- **当某个通信原语（如 `lmhead_all_to_all` 内的 `view(world_size, -1, V/P)` + `dist.all_to_all_** [PR12923]
- **当用本地自定义实现替换上游 import 的实现时，如果本地实现不依赖版本差异，应移除为兼容上游版本而保留的条件分支（如 `vllm_version_is`），** [PR13048]
- **当一个 Ascend 内核同时安装给 main 与旧版本（如 v0.26.0）调用方，而两者的输出契约不同（main 传非空输出缓冲，旧版本传 `None` 并** [PR13477]
- **在进行软件版本检查时，应使用数值比较（如 `>=`）而非字符串前缀匹配（如 `startswith`），避免因版本号变化导致逻辑遗漏。** [PR2619]
- **当修改 patch 子目录下的文件时，必须同步更新 patch/__init__.py 中的导出和引用，确保模块一致性。** [PR4975]
- **升级依赖版本或适配上游变更时，涉及数组/元组下标访问的代码必须仔细核对下标是否正确，尤其是上游接口发生变更时。** [PR5053]
- **安装脚本中的 CANN 版本号和下载链接应使用变量（如 `${CANN_VERSION}`）而非硬编码值，以便在升级 operator 包时无需修改脚本代码。同** [PR6989]
- **在需要区分不同 vLLM 版本行为时，应使用 `vllm_version_is` 工具函数进行版本判断，而不是硬编码或其他方式，以确保版本升级时的平滑过渡。** [PR7342]
- **在 main2main 上游同步过程中，需确保 Triton kernel 在不可用平台（如 310P）上有正确的 fallback 实现，版本条件 guard** [PR9726]
- **模型部署文档中的版本引用应推荐使用最新的 release candidate 版本或正式版本，而非硬编码特定旧版本号。当版本升级时，文档应同步更新。同时，sup** [PR10616]
- **当文档包含多个硬件平台（如 A3/A2）的部署示例时，应使用 tab-set 配合 sync-group/sync 创建同步标签，确保用户选择某一平台后，页面所** [PR10693]
- **模型部署教程中，多节点（Node 0 / Node 1）的配置应使用 tab-set 语法分开展示，避免混淆。文档章节编号应连续完整，性能调优部分应按模板规范编** [PR11686]
- **在升级 vLLM 版本时，必须逐一检查现有的兼容性 patch 是否在新版本中仍然需要。已在上游修复的 patch 应及时移除，避免维护冗余代码。** [PR10476]
- **在升级 vLLM 版本时，应确保设备初始化、patch 目标等与上游实现保持一致。避免使用与上游冲突的环境变量设置方式，patch 应选择更精确的目标类。** [PR11227]
- **vllm-ascend 中通过 monkey-patch 修改上游行为的每个 patch，都应在 `vllm_ascend/patch/__init__.py`** [PR13477]
- ****检查点**: 升级依赖版本（如 torch-npu）后，应检查并删除因旧版本兼容而保留的冗余 mock 函数或条件分支。; 删除冗余代码时，需确保相关参数和** [PR3896]
- ****检查点**: 升级 CANN 等核心依赖版本时，不应随意修改与版本升级无直接关系的测试配置（如 HCCL_BUFFER_SIZE）** [PR4350]
- **在 CI workflow 文件中引用的 vllm 版本必须与项目当前支持的版本保持同步，版本升级时需同步更新所有相关的 workflow 配置文件。** [PR6927]
- **升级 Python 版本（如 3.11 → 3.12）时，需要同步更新 Dockerfile、setup.py、requirements-dev.txt 和文档** [PR9558]
- **PR 中不应保留被注释掉的旧代码，应在提交前清理。对于需要迁移到上游的代码，应添加 TODO 标注迁移计划。** [PR10268]
- **当文档站点为发布候选版（RC）而不是 GA 稳定版时，header.html 中"latest stable release"导航链接和标注文案必须继续指向最近** [PR10103]

## 性能优化 (17 条)

- **新增的 Triton kernel 必须配套单元测试。使用 `tl.constexpr` 修饰的变量应确保在实际运行中不会频繁变化，否则会导致 Triton J** [PR11467]
- **在发起 HCCL/P2P 张量传输前，应将本地张量 `numel` 与对端下发的 shape manifest 比对，不匹配时 fail-fast 中止并走 f** [PR12885]
- **当性能优化依赖于对数据布局的隐含假设（如连续性、有序性）时，必须添加显式检查并在假设不满足时回退到安全路径。** [PR13182]
- **当使用 Triton kernel 作为优化路径时，必须在调用处检查函数是否可用（非 None），并在不可用时 fallback 到标准 PyTorch 实现。** [PR13509]
- **在 logger 调用中应使用 `%` 格式化而非 f-string，因为 f-string 会在日志级别未启用时仍然执行字符串格式化，造成不必要的性能开销。** [PR3811]
- **在使用 permute 等可能产生非连续内存的操作后，如果后续需要高效访问，应添加 contiguous() 调用。** [PR4658]
- **当只需要更新 tensor 中的单个元素时，应直接通过索引赋值（如 `tensor.gpu[idx] = value`），而不是使用 `copy_to_gpu(** [PR7858]
- **当某项性能优化特性（如 fullgraph/cudagraph）已在主线合入并可用时，官方教程文档的启动命令应启用该特性，而非使用 `enforce_eager** [PR12491]
- **CI 缓存的 cache key 需要选择合适的文件粒度。粒度太粗（如仅用 .so 文件）可能导致缓存命中不准确；粒度太细则缓存命中率低。使用 C++ 源代码文** [PR10490]
- **当某个计算结果只有少数固定值且被频繁使用时，应预先计算这些值并存储在查找表中，通过查表代替实时计算，以减少运行时开销。** [PR10590]
- **在 Triton kernel 中使用的常量参数，应确认其在实际运行中是否保持不变，变化是否会导致 kernel 重编译，从而影响性能。** [PR10741]
- **在高频调用的函数（热路径）中，应避免进行可以预计算或缓存的冗余计算。将计算移到调用方或使用缓存机制，以减少每次调用的开销。** [PR11195]
- **Triton 的 `@triton.jit(do_not_specialize=[...])` 装饰器用于标记"每次调用可能变化、不应触发重新编译"的参数。对于** [PR13327]
- **在 Triton kernel 中，由模型架构决定且运行期间不变的参数（如 `index_head_count`、`top_k`、`head_dim`）应声明为** [PR13532]
- **如果多个层或多次调用使用相同的张量（如 `torch.arange`），应只构造一次并传递，而非在每次调用中重复创建，以减少不必要的计算开销。** [PR2373]
- **在 Triton 内核中，对于在运行时才会确定值的变量（如 `num_tokens`、`max_num_bad_words`），必须使用 `do_not_spe** [PR8030]
- **当 Triton kernel 包含多个独立的 grid-stride loop（各 loop 处理不同范围的工作），grid size 应按最大工作范围 `m** [PR13191]

## 构建与部署 (16 条)

- **自定义算子的安装包（run/whl）应上传到项目自有的 OBS 仓库（如 `vllm-ascend.obs.cn-north-4.myhuaweicloud.c** [PR10034]
- **prefill 和 decode 的 metadata 构建应遵循相同的模式和位置。如果某个参数在 decode 路径中通过 Builder 类添加，prefi** [PR5704]
- **在移除旧版本支持时，需同步更新所有版本相关的配置项，包括 CANN 版本号、pip 包版本号、Dockerfile 中的 VLLM_TAG 等。文档中的路径引用** [PR7153]
- **Python 中 `==` 优先级高于 `or`（以及 `and`），因此 `x == A or B` 实际解析为 `(x == A) or (B)`，当右操作** [PR9280]
- **模型部署教程文档应按照模板包含推荐配置表（表格形式），并在版本信息处明确建议使用最新的 release candidate 或 official 版本。** [PR10840]
- **已发布版本的 release-note 是历史文档，一旦发布不应修改。移除功能或不支持某模式时，应在当前/下一版本的 release-note 中声明，而非修改** [PR12031]
- **文档和版本策略文件中不应提前包含尚未正式发布的版本信息。版本信息应在正式发布时才添加到文档中。** [PR12042]
- **在文档配置中指定 pip 安装版本号时，不应使用 `v` 前缀（如 `v0.10.2`），因为 pip 版本标准格式不包含该前缀，错误的前缀会导致安装命令失败。** [PR2921]
- **教程文档中不应包含已不再需要的依赖安装步骤，部署命令应包含完整的参数配置。** [PR4607]
- **模型部署文档中的启动命令必须包含所有必要参数，特别是影响功能正确性的参数（如 `--mamba-cache-mode align` 对于 prefix cach** [PR7313]
- **CI 构建 workflow 中的 soc_version 参数必须与目标硬件匹配，未准备好的平台不应纳入镜像发布流程。** [PR10692]
- **在 Dockerfile 中安装 Python 包时，应使用 `python3 -m pip install --no-cache-dir` 而非直接使用 `p** [PR10871]
- **Dockerfile 中使用 `wget`/`curl` 下载构建依赖时，须配置重试次数（`--tries`）、连接超时（`--timeout`/`--conn** [PR13063]
- **在编辑 Kubernetes 资源 YAML 模板（如 Jinja2）时，容器 `resources` 块的 `limits`/`requests` 与 `vo** [PR14190]
- **当相同的依赖安装 RUN 块在多个 Dockerfile 中重复出现时，应抽离为单一共享 shell 脚本（如 `scripts/install_daily_d** [PR13063]
- **当多个 shell 脚本包含几乎相同的代码块时，应提取为独立的共享脚本文件（通过 `source` 引入），创建单一数据源，避免维护时需要多处修改。** [PR13167]

## 测试与CI (15 条)

- **add op test.** [PR4595]
- **在代码重构过程中，新增的索引访问应包含越界校验，assertion 应包含清晰的错误说明。** [PR4628]
- **The code can be optimized through predicate statements, resulting in more concis** [PR8508]
- **当多个测试文件测试同一功能的不同配置时，应合并为一个文件并使用 `pytest.mark.parametrize` 进行参数化，避免测试文件冗余。** [PR9763]
- **新增的 CI 工具或开发者工具应放在 `tools/` 目录下的对应子文件夹中（如 `tools/bisect/`），而非散落在 `tests/` 或其他非标准** [PR10564]
- **在 `tests/e2e/nightly/` 目录下新增的测试用例配置文件，必须同时在 `.github/workflows/configs/nightly_c** [PR11022]
- **添加新的 nightly 测试模型配置时，必须同步将模型添加到 `.github/workflows/misc/model_dataset_list.json`** [PR11856]
- ****检查点**: 单卡测试用例应放在 `tests/e2e/singlecard/` 目录下，多卡测试用例放在 `tests/e2e/multicard/` 目** [PR4039]
- **在 CI 配置文件中跳过或移动测试时，应保持测试分类结构的清晰性，避免随意改变测试的组织位置。对于代码修复类变更，应拆分到独立 PR 以保持 PR 职责单一。** [PR4820]
- **PR 合入前必须移除所有临时测试代码和调试配置。CI 配置文件应只包含正式的、经过验证的配置。** [PR6503]
- **上游兼容性测试工作流不应在每次 PR 提交时触发，应仅通过定时任务（schedule）触发，避免不必要的 CI 资源消耗。工作流文件命名应清晰反映其用途。** [PR7946]
- **在 CI 工作流中，E2E 测试步骤的 `continue-on-error` 必须设置为 `false`，确保测试失败时 CI 流水线能够正确报错并阻止合入。** [PR8060]
- **在使用 smart e2e 测试的 CI 工作流中，必须设置 `ut_tracker: true`，确保 e2e-filter 功能正常工作，避免意外跳过测试过** [PR8777]
- **```suggestion @pytest.mark.skipif(not is_310p_hw(), reason="Tested separately on** [PR9465]
- **在 Triton kernel 中，若 `do_not_specialize` 列表中的参数被用于整数除法 `//` 或取模 `%` 运算，会导致编译为动态 G** [PR13532]

## 未分类 (11 条)

- **添加功能限制时，应确保限制是必要的。如果评审者质疑限制的必要性，应重新评估并移除不必要的限制。** [PR14023]
- **当资源仅适用于某个特定子场景时，条件判断应精确匹配该子场景，而非仅检查外层条件。** [PR14250]
- **@zzzzwwjj  @whx-sjtu** [PR5939]
- **If _get_fia_params already returns actual_seq_lengths_kv, the subsequent assignm** [PR9165]
- **need reconstruct this.** [PR9279]
- **Why did you add `Hunyuan-A13B-Instruct` and `Minitron-8B-Base`?  Just rebase you** [PR8537]
- **Missing explanation for `invalid`, `wontfix` label in here, which are part of th** [PR8968]
- **why not move this to L132?** [PR9037]
- **rebase to main and remove this section directly** [PR9310]
- **remove the useless annoation code.** [PR9528]
- **不要用 `is`/`is not` 对 list、tuple 等序列对象做内容等价性判断；身份比较仅在对象同一性确实重要时使用，且应确保对象不会被复制/切片。** [PR13169]

## KV Cache与存储 (10 条)

- **PCP（Prefill-Chunked-Prefill / Pipeline Context Parallel）相关的工具函数应统一放在 `pcp_utils`** [PR10580]
- **在计算 Mamba KV cache group 的 block table 大小时，speculative blocks 应在 max() 比较之后添加，而非** [PR11466]
- **在 model runner 中处理 KV cache 时，sparse C8 场景下 SFA 层只有 k_cache（无 v_cache），需要用 `(k_c** [PR11647]
- **在 `_dummy_run` 中重置 block table 的 slot_mapping 时，应仅在非 graph capture 场景下执行。在 graph** [PR11774]
- **当同一函数需要支持多种模式（如 `mamba_cache_mode` 的 `align`/`none`），且不同模式下数据形状/长度语义不同（如对齐模式 `re** [PR14254]
- **在 Ascend Store 的 KV transfer 实现中，Hybrid 和 Mamba 模型的前缀缓存对齐需要确保 config_data 配置和 po** [PR9533]
- **Shell 脚本中 if/else/fi 块内的代码必须缩进到对应层级，使控制流结构清晰可读。else 块内的代码缩进应与 then 块内的代码保持一致。** [PR13167]
- **在 KV cache 传输中，当存在 block_stride 时，应使用 stride 方式发送块，而非逐块发送。这样可以合并传输任务，充分利用带宽，提升传输** [PR10342]
- **不要用模块级可变全局变量（如 `_xxx_available = True`）缓存 custom operator 的能力探测结果。一旦探测失败将全局永久置为 ** [PR12592]
- **对实现了 `__enter__`/`__exit__` 副作用的上下文管理器编写 UT 时，`with` 块内断言须反映 `__enter__` 后的状态，re** [PR12885]

## 类型与接口设计 (6 条)

- **不同量化类型（mxfp4, w4a8 等）的 dst_type 必须严格匹配，不应混用。当新量化类型与旧路径存在性能差异时，应保持独立路径分别处理。** [PR10749]
- **在投机解码的 SFA（dsa_cp）模式中，`build_for_drafting` 应为每个 draft step 使用独立的 seq_lengths buf** [PR10825]
- **在添加新模型支持时，应确保所有接口调用（如 `MultiGroupBlockTable.__init__`）的参数与现有框架一致，避免因参数不匹配导致的 `Ty** [PR2917]
- **MoE 通信类型不应硬编码，应通过 `get_forward_context().moe_comm_type` 统一获取，以支持不同通信策略的灵活切换。** [PR3532]
- **在 Triton kernel 中，`tl.store` 会自动进行 dtype 转换，不需要在存储前显式调用 `.to(tl.bfloat16)` 等硬编码类** [PR6730]
- **Python 的 `os.environ` 字典只接受字符串值，赋值整数或其他类型会抛出 `TypeError: strval must be a string** [PR9820]

## 内存与张量管理 (5 条)

- **访问张量特定维度时，若该维度始终为最后一维，应使用负索引 `size(-1)` 而非绝对索引 `size(N)`，避免张量秩变化时越界崩溃。** [PR12850]
- **在上下文管理器（context manager）中，资源清理逻辑（如内存释放、workspace 回收）必须放在 `try...finally` 块的 `fin** [PR8111]
- **after dummy_run, the _ATTN_KEYS_BUFFER  will be []. The condition should be:    ** [PR9322]
- **从 socket 流式接收并拼装 JSON 时，不应在每个 chunk 到达后全量解析（$O(N^2)$），而应仅在缓冲区以闭合符 `}`/`]` 结尾或连接关** [PR12885]
- **当 kernel 通过原地更新消费状态张量时，不应在每次调用时分配不会被使用的输出状态张量，应通过标志位控制避免 materializing 多余张量。** [PR13836]

## 并发与分布式 (3 条)

- **在测试 data parallel (DP) 和 context parallel (CP) 场景时，应使用 `DPVllmRunner` 而非普通的 `Vll** [PR12034]
- **在数据并行（DP）环境中，speculative decoding 的 proposer 必须通过 `_sync_metadata_across_dp` 同步所** [PR9689]
- **在 speculative decoding 的 base proposer 中，`torch.npu.Stream()` 的初始化应放在正确的位置，确保在异步** [PR9735]

## 安全与健壮性 (2 条)

- **当存在简洁直接的解决方案时（如直接调用 eager mode），应避免引入复杂的验证逻辑。简洁的实现更容易维护和理解。** [PR11327]
- **GitHub Actions 中 `workflow_dispatch` 输入项的 `default` 值与下游 `${{ inputs.xxx || '<fa** [PR13063]

